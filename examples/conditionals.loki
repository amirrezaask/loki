#load "modules/print.loki"

main :: () int {
   a := 1;
   if (a > 10) {
      printf("a is bigger than 10\n");
   } else if ((a < 10) && (a > 5)) {
      printf("a is between 10 and 5\n");
   } else {
      printf("a is less than 5\n");
   }
}
