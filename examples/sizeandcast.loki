main :: () int {
    a := Human {
        age = 10,
        name = "amirreza"
    };
    asize := sizeof(a);
    aptr := cast(&a, *Human); // just to see how cast works
}

Human :: struct {
    age: int,
    name: string
}