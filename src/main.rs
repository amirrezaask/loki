mod tokenizer;
mod parser;
mod tests;

fn main() {
    // let tokens = tokenizer::tokenize("fn_name(fn_name2(fn_name3(12)))").unwrap();
    // println!("{:?}", parser::Parser::new(tokens).next_expr().unwrap());
}



