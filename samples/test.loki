#load "base.loki";


Direction :: enum {
    Up,
    Down
};

Vector :: struct {
    x: int,
};

Game :: struct {
    a: Vector,
    direction: Direction,
};

main :: fn() int {
    g : Game;
    g.direction = Direction.Up;
    a := Direction.Up;
    printf("hello world! %d", 2);
};
