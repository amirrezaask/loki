#load "base.loki"

State :: enum {
    Alive,
    Dead
}

Human :: struct {
    age: int,
    name: string,
    // state: State,
}
main :: () int {
    human := Human {
        age = 24,
        name = "amirreza",
    };

    human_ptr := >>human;
    human2 := <<human_ptr;
    human_age := human_ptr.age;
};
