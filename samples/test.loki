#load "base.loki"

Human :: struct {
    age: int,
    name: string,
}

main :: () int {
    human := Human {
        age = 24,
        name = "amirreza",
    };

    human_ptr := &human;
    // age := human_ptr.age;
};
