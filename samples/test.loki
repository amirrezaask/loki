#load "base.loki";
Human :: struct {
    age: int,
    name: string,
}
main :: () int {
    human_ptr := &Human {
        age = 24,
        name = "amirreza",
    };

    age := (*human_ptr).age;

    printf("human_ptr age is %d\n", age);
    return 0;
};
